import { artspaces } from 'artspaces/helpers/artspaces';
import { module, test } from 'qunit';

module('Unit | Helper | artspaces');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = artspaces([42]);
  assert.ok(result);
});
