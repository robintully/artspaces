import { handlebars } from 'artspaces/helpers/handlebars';
import { module, test } from 'qunit';

module('Unit | Helper | handlebars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = handlebars([42]);
  assert.ok(result);
});
