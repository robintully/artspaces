import { ifequal } from 'artspaces/helpers/ifequal';
import { module, test } from 'qunit';

module('Unit | Helper | ifequal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifequal([42]);
  assert.ok(result);
});
