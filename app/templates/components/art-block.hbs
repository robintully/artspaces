<div class="col-md-4 art-block" style="text-align:center;
    ">
{{#if hovered}}
  <span class="helper"></span>
<img src="{{thing.img_link}}" style="max-width:95%;max-height:95%">
  <span class="art-info">

  {{#link-to "arts.art" thing.id}}
<h3> <a data-toggle="modal" href="#modal">{{thing.name}}</a></h3>
 {{/link-to}}


  
    <p>{{thing.description}}</p>
    <p>by {{thing.user.name}}</p>
  </span>
{{else}}
  <span class="helper"></span>
  <img src="{{thing.img_link}}" style="max-width:95%;max-height:95%">
{{/if}}
</div>